<div class='container col-12 px-5 px-sm-0'>
    <div class="col-md-10 mx-auto">
      <div class="row">
        <h1 class="mt-4 text-center bg-light">商品一覧</h1>
        <div class="col text-right">
          <%= link_to new_admin_item_path,class: 'btn btn-default' do %>
           <i class="fas fa-plus-circle fa-2x"></i>
          <% end %>
        </div>
      </div>

      <table class='table'>
        <thead>
          <tr>
            <th>商品ID</th>
            <th>商品名</th>
            <th>税抜価格</th>
            <th>ジャンル</th>
            <th>ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @items.each do |item| %>
          <tr>
            <td><%= item.id %></td>
            <td><%= link_to item.name, admin_item_path(item)  %></td>
            <td><%= item.price %></td>
            <td><%= item.genre.name %></td>
            <td><%= item.is_active_i18n %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @items %>
    </div>
  </div>
</div>